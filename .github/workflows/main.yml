name: Say Hello

on:
  workflow_dispatch:
    inputs:
      name:
         description: "name to grate"
         default: "gust"
         required: false

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - run : |
          echo "hey ${{ inputs.name }} "
          echo "what here"
    
      - run : echo "weeeee" 

  second-job:
    needs: first-job
    runs-on: ubuntu-22.04
    steps:
      - run : echo "job 2"

  thrid-job:
    runs-on: ubuntu-22.04
    steps:
      - run : echo "job 3"
  
